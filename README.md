# Sorting-Algorithms

Merge Sort
- Keep splitting the list in half until we have single items 
- Merge them back together in sorted order


Quick Sort
- Pick an element from the list (pivot)
- Put all smaller elements on the left, bigger on the right 
- Keep repeating the same process on each side 


Difference
- Quick sort: sort while splitting
- Merge sort: split first, sort later 